# Grazhda Configuration
# This file is auto-generated during installation
# Modify as needed for your environment

# Editor used by `grazhda config --edit`.
# Resolved in order: this field → $VISUAL → $EDITOR → vi
editor: vim

# Dukh gRPC Server Configuration
dukh:
  host: localhost
  port: 50501
  monitoring:
    period_mins: 5

# Zgard Configuration
zgard:
  config: {}

general:
  install_dir: ${GRAZHDA_DIR}
  sources_dir: ${GRAZHDA_DIR}/sources
  bin_dir: ${GRAZHDA_DIR}/bin

workspaces:
  - name: default
    path: /home/jake/ws
    clone_command_template: git clone --branch {{.Branch}} https://github.com/grazhda/{{.RepoName}} {{.DestDir}}
    # structure controls how repository names containing "/" are mapped to local
    # directories inside each project folder.
    #   tree (default) — preserves full path:  org/pack/repo → <project>/org/pack/repo
    #   list           — uses shortest unique suffix: org/pack/repo → <project>/repo
    #                    (falls back to pack/repo, then org/pack/repo if shorter names exist)
    structure: tree
    projects:
      - name: ws-project1
        branch: main
        repositories:
          - name: test-repo-1
          - name: test-repo-2
          - name: test-repo-1
            local_dir_name: test-repo-1-copy
          - name: test-repo-3
            branch: dev
      - name: ws-project2
        branch: main
        repositories:
          - name: test-repo-1
          - name: test-repo-2
          - name: test-repo-3
  - name: secondary
    path: /home/jake/secondary_ws
    clone_command_template: git clone --branch {{.Branch}} https://github.com/grazhda/{{.RepoName}} {{.DestDir}}
    structure: tree
    projects:
      - name: secondary-project1
        branch: main
        repositories:
          - name: test-repo-1
          - name: test-repo-2
